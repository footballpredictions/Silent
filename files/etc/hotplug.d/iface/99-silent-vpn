#!/bin/sh
# LAN prefix can change (192.168.1.1 → 10.0.0.1). Refresh *.silent.vpn.

[ "$ACTION" = "ifup" ] || [ "$ACTION" = "ifupdate" ] || exit 0
[ "$INTERFACE" = "lan" ] || exit 0
[ -x /usr/sbin/silent-vpn-ctl ] || exit 0
/usr/sbin/silent-vpn-ctl apply-dns >/dev/null 2>&1 || true
