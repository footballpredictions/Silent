# Gradle / Android Studio
app/.gradle/
.gradle/
app/build/
build/
.idea/
*.iml
local.properties
app/local.properties

# Release builds (локально)
*.apk
releases/

# Keystore (локально, не в git)
keystore/*
!keystore/keystore.properties.example

# Secrets
*.env
.env.deploy
*.keystore

# Logs
*.log

# Native libs (собираются локально / CI: libclient, libolcrtc, libhev-socks5-tunnel)
app/src/main/jniLibs/

# Legacy olcrtc binaries in assets (используем jniLibs/libolcrtc.so)
app/src/main/assets/olcrtc/

# OS
.DS_Store
Thumbs.db
